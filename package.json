{
  "private": true,
  "packageManager": "pnpm@9.15.9",
  "scripts": {
    "dev": "pnpm run dev:engine & pnpm run dev:web",
    "dev:engine": "pnpm --filter=thicc-svg run dev",
    "dev:web": "pnpm --filter=thicc-svg-web run dev",
    "build": "pnpm --filter=thicc-svg run build && pnpm --filter=thicc-svg-web run build",
    "build:engine": "pnpm --filter=thicc-svg run build"
  },
  "engines": {
    "node": ">=18"
  }
}
